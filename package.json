{
  "name": "boxoffice-platform",
  "private": true,
  "version": "1.7.0",
  "workspaces": ["server"],
  "scripts": {
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server start",
    "check": "node scripts/validate-deployment.mjs",
    "build:client": "node scripts/build-pages.mjs client",
    "build:admin": "node scripts/build-pages.mjs admin",
    "build:owner": "node scripts/build-pages.mjs owner"
  },
  "engines": {
    "node": ">=20"
  }
}
